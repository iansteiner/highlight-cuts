services:
  web:
    build: .
    container_name: highlight-cuts-web
    ports:
      - "8000:8000"
    volumes:
      # Mount the local directory containing source videos
      - /mnt/data/highlights/input:/app/data
      # Mount the output directory to persist generated highlights
      - /mnt/data/highlights/output:/app/output
      # Mount source for development
      - ./src:/app/src
    command: uv run uvicorn highlight_cuts.web:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - HIGHLIGHT_CUTS_OUTPUT_DIR=/app/output
    restart: unless-stopped
